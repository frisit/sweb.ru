{% extends 'base.html.twig' %}

{% block body %}
    <p><a href="{{ path('blog_posts') }}">< Вернуться к постам</a></p>
    <p><a class="btn btn-warning" href="{{ path('blog_post_edit', {'slug': post.slug}) }}">Редактировать</a></p>
    <p><a class="btn btn-danger" href="{{ path('blog_post_delete', {'slug': post.slug}) }}">Удалить</a></p>
    <p>id: {{ post.id }}</p>
    <p>title: {{ post.title }}</p>
    <p>text: {{ post.body }}</p>
{% endblock %}